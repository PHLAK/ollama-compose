services:
  cpu: {}

  amd:
    devices: [/dev/dri:/dev/dri, /dev/kfd:/dev/kfd]

  intel:
    devices: [/dev/dri:/dev/dri]

  nvidia:
    gpus: all

  vulkan:
    devices: [/dev/dri:/dev/dri, /dev/kfd:/dev/kfd]
    environment:
      OLLAMA_VULKAN: 1
